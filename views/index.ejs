<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
</head>
<body>
  <h1>URL Shortener</h1>
  
  <form action="/shorten" method="POST">
    <label for="original_url">Enter your URL:</label>
    <input type="text" id="original_url" name="original_url" required placeholder="Enter URL here" />
    <button type="submit">Shorten URL</button>
</form>

<% if (short_url) { %>
  <h2>Shortened URL:</h2>
  <p><a href="/<%= short_url %>" target="_blank">http://localhost:3000/<%= short_url %></a></p>
<% } %>

</body>
</html>  